import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    display: "flex",
    backgroundColor: '#26c6da',
  },
  titleStyle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold'
  }
});